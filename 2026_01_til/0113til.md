# 2026/01/13

久しぶりにRubyアルゴリズム問題に挑戦した。

### 行入力】1,000 行の入力

```ruby
(0..999).each do
    input_line = gets.chomp.split.map(&:to_s)
    puts input_line
end
```

というコードを書いたが、`1000.times do`の方が適切だとAIに指摘されました。  
異論ありません。

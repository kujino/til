# 2025/11/20, 22の日報
total: 470 hour
## アプリ開発
### デザイン面の実装
- ヘッダーがスクロールに追従するようにした  
- フッターを実装した  
### 投稿詳細は投稿者しか見れない仕様にエラーメッセージ
カリキュラムでは、投稿編集の際コントローラーで
```ruby
@post = current_user.posts.find(params([:id])
```
としておくことでURLベタ打ちされたとしても`ActiveRecord::RecordNotFoundエラー`が出て表示されないという実装をしたが、個人的にそこを仕様で解決したいと思いメソッドを作成  

@postのuser_idがcurrent_user.idのと一致しない場合はブロックしフラッシュメッセージで権限がないことをお知らせする。

### i18nを使った日本語化
コードが少ないうちに日本語化をしておこうと思いつき実装中

